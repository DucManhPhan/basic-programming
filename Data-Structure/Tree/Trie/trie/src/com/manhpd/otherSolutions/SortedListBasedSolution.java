package com.manhpd.otherSolutions;

import java.util.Collections;
import java.util.List;

public class SortedListBasedSolution {

    public static void main(String[] args) {
        String[] strings = {
                "apple", "ant", "bin", "back", "ball", "big", "brick", "bribe", "broke", "bride", "brag", "bridge", "bright", "brisket", "yellow", "wood"
        };

        List<String> results = sortedListBasedSolution(strings);
        System.out.println("Results: " + results);
    }

    /**
     * Sort the input that has array list of strings first.
     * Using binary search to find the correct incremental prefix
     *
     * @param input
     * @return
     */
    private static List<String> sortedListBasedSolution(String[] input) {


        return Collections.emptyList();
    }
}
